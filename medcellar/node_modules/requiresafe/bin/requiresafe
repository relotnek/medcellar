#!/usr/bin/env node
var CLI = require('../lib/cli');


// Run the library
CLI.run(function (err, response) {

    if (err) {
        console.error(err);
        process.exit(1);
    }

    console.log(response);
});
