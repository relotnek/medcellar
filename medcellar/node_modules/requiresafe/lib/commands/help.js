/*
Shows this menu
*/

var commands = require('../get-help');


// Just build up some help text and return it
module.exports = function (args, callback) {
    var helpText = commands.map(function (command) {
            return command.help;
        });
    
    callback(null, helpText.join('\n'));
};
