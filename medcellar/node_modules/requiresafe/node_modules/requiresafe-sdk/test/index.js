var test = require('tape');
var SDK = require('../rs-sdk');


test('init', function (t) {
    var api = new SDK();
    t.ok(api.login, 'has login method');
    t.ok(api.logout, 'has logout method');
    t.end();
});
